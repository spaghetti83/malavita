version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      # Opzionale: Monta il Caddyfile se vuoi modificarlo senza ricostruire
      - ./Caddyfile:/etc/caddy/Caddyfile
    restart: always